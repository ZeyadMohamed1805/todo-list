export type THeaderBadgeProps = {
  props: {
    toggleDropdown: () => void;
    arrowClassName: string;
  };
};

export type THeaderDropdownButtons = {
  props: {
    closeDropdown: () => void;
  };
};