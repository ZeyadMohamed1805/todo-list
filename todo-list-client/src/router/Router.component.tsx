import { Routes } from 'react-router-dom';
import { ApplicationRoutes } from './Router.blocks';

const Router = () => {
  return (
      <Routes>
        {ApplicationRoutes}
      </Routes>
  );
};

export default Router;
